# 旧 hardware-configuration.nix 已迁移到 hosts/<machine>/hardware.nix
# 硬件配置无敏感信息，flake 需要 git 跟踪，正常提交即可
# 每台机器的 hardware.nix 在自己对应主机的目录里生成
.mimocode
# 运行日志不入库（btop.log 曾被误跟踪，现已 git rm --cached 取消跟踪）
dotfiles/common/btop/btop.log

# 每机本地状态不入库: fish 会自建 fish_variables（含历史大小、pager 颜色等），
# 共享它会让 sync/diff 永久脏，且把一台机器的状态推给另外两台
dotfiles/common/fish/fish_variables

# agent 工具私有状态目录不入库 (AGENTS.md 是唯一的跨工具规则载体)
.claude/
.dsh-meow/


