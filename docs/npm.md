### before use npm in nix(new)

`npm set prefix ~/.npm-global`
```
cat .fishrc | rg npm
set -p PATH ~/.local/bin ~/.cargo/bin ~/.npm-global/bin  
```

### how 2 beat gfw

```bash
npm i -g cnpm
```



